[package]
name = "jsync"
version = "0.1.0"
edition = "2021"
description = "A simple file synchronization tool using rsync"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"